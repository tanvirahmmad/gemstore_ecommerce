import 'package:flutter/material.dart';

class HangOutParty extends StatelessWidget {
  const HangOutParty({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [

      ],
    );
  }
}
